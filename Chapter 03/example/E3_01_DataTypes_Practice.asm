; ============================================================
; [시험 대비 빈칸 채우기] 3장 - 데이터 타입, DUP, 리틀 엔디안
; 빈칸( ______ )을 채우세요. 정답은 E3_01_DataTypes_Answer.asm 참고.
; ============================================================

.386
.MODEL flat, stdcall
.STACK 4096

ExitProcess PROTO, dwExitCode:DWORD

.DATA

; ──────────────────────────────────────────────────────────
; [문제 1] 아래 설명에 맞는 데이터 타입을 채우세요.
;
;   타입         크기      범위
;   (1)______    1바이트   0 ~ 255
;   (2)______    1바이트   -128 ~ 127
;   (3)______    2바이트   0 ~ 65,535
;   (4)______    4바이트   0 ~ 4,294,967,295
;   (5)______    4바이트   -2,147,483,648 ~ 2,147,483,647
; ──────────────────────────────────────────────────────────

; [문제 2] 데이터 정의 — 빈칸을 채우세요.

    val1  (6)______  10h       ; 부호 없는 1바이트 변수 
    val2  (7)______  -50       ; 부호 있는 1바이트 변수
    val3  (8)______  ?         ; 부호 없는 4바이트, 미초기화
    wArr  (9)______  100, 200, 300  ; 2바이트 배열 

; ──────────────────────────────────────────────────────────
; [문제 3] DUP 연산자 — 빈칸을 채우세요.

    ; 0으로 초기화된 BYTE 10개
    buf1  BYTE  (10)______ DUP( (11)______ )

    ; 미초기화 DWORD 5개
    buf2  DWORD (12)______ DUP( (13)______ )

    ; 중첩 DUP: 2×3 = 6개의 0FFh (BYTE)
    buf3  BYTE  (14)______ DUP( (15)______ DUP(0FFh) )

; ──────────────────────────────────────────────────────────
; [문제 4] $ 연산자 — 배열 크기 자동 계산

    list  BYTE   10, 20, 30, 40, 50
    ListSize  = ( (16)______ - list ) 
    ; ListSize = (17)______

    wList  WORD  100, 200, 300, 400
    wListSize = ($ - wList) / (18)______
    ; wListSize = (19)______

.CODE
main PROC

; ──────────────────────────────────────────────────────────
; [문제 5] 리틀 엔디안 — 빈칸을 채우세요.
;
;   DWORD 12345678h 가 주소 0200h 부터 저장될 때:
;
;   주소 0200h : (20)______ 
;   주소 0201h : (21)______
;   주소 0202h : (22)______
;   주소 0203h : (23)______
; ──────────────────────────────────────────────────────────

; [문제 6] 16진수 리터럴 규칙 — 올바름 / 오류를 고르세요.
;   A)  val BYTE  0A3h      ; (24)______ 
;   B)  val BYTE  FFh       ; (25)______  이유: ______ 16진수의 맨 앞자리가 문자열이면 0을 사용해야한다. 그렇지 않으면 상수로 취급받지 못함. 변수나 레이블로 취급받을지도...
;   C)  val BYTE  0FFh      ; (26)______
;   D)  val BYTE  0Ah       ; (27)______

; [문제 7] .DATA? 의 목적:
;   (28)______ 을/를 줄이기 위해 초기화하지 않을 대용량 데이터를 선언하는 세그먼트
;   .DATA?에 적합한 초기값 표현:  (29)______

    INVOKE ExitProcess, 0
main ENDP
END main
